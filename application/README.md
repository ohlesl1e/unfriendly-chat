# This is the application directory for Unfriendly Chat
---

### How to run

`npm run req` - install dependencies
`npm run dev` - run dev server

- frontend, dir: /client, port: 3000
- backend, dir: /server, port: 4000